# Required by the built-in Pi MiniMax provider.
MINIMAX_API_KEY=

# Optional. Defaults to MiniMax-M2.7, Pi's current default MiniMax model.
MINIMAX_MODEL=MiniMax-M2.7

# Optional. Use minimax-cn if your key is for the China endpoint.
MINIMAX_PROVIDER=minimax
